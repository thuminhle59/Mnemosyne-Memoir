# Secrets — NEVER commit (contest repo is public)
.env
.env.*
!.env.example
.greennode.json
.agentbase-state.json
.gh-config/
*.key
*.pem
credentials.json
*.credentials.json

# AgentBase skills (dev tooling, not runtime — không cần submit)
.claude/
.agents/
.agentbase/

# Python
venv/
__pycache__/
*.pyc
.pytest_cache/

# Build / OS
.DS_Store
*.db
dist/
build/
*.egg-info/

# Media artifacts từ demo (dữ liệu, không push)
*.wav
*.aiff
*.mp3
*.docx
*.pdf

# Local demo media (large, do not commit)
demo-meetings/

# Local design mockups / reversible UI drafts (keep locally, do not commit)
web/backup-designs/
web/*mockup.html
web/upload-progress-mockup.html
web/terminology-edit-mockup.html
web/meeting-groups-mockup.html
